/* eslint-disable */
import BackdropTv from './../components/TV/BackdropTv';

const Tv = (props) => {
    return (
        <div style={{ backgroundColor: '#FFFFFF' }}>
            <BackdropTv object={props} />
        </div>
    );
};

export default Tv